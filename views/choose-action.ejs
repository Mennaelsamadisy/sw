<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Welcome  | Event Planner</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
  <script src="https://kit.fontawesome.com/a2e0e6cfd5.js" crossorigin="anonymous"></script>
  <style>
    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      padding: 0;
      font-family: 'Inter', sans-serif;
      background: linear-gradient(to right, #e9f1ff, #f7faff);
      color: #333;
    }

    nav {
      background-color: #ffffff;
      padding: 20px 40px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    nav .brand {
      font-size: 24px;
      font-weight: 700;
      color: #0f65e6;
    }

    nav .user {
      font-weight: 500;
      color: #555;
    }

    .hero {
      text-align: center;
      padding: 60px 20px 30px;
    }

    .hero h1 {
      font-size: 36px;
      margin-bottom: 10px;
    }

    .hero p {
      font-size: 18px;
      color: #666;
    }

    .container {
      display: flex;
      justify-content: center;
      gap: 40px;
      flex-wrap: wrap;
      max-width: 1000px;
      margin: 40px auto;
      padding: 0 20px;
    }

    .card {
      width: 280px;
      background-color: white;
      border-radius: 18px;
      padding: 30px 25px;
      text-align: center;
      text-decoration: none;
      color: inherit;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.08);
      transition: all 0.25s ease;
    }

    .card:hover {
      transform: translateY(-8px);
      box-shadow: 0 16px 30px rgba(0, 0, 0, 0.15);
    }

    .card i {
      font-size: 48px;
      color: #007BFF;
      margin-bottom: 20px;
    }

    .card h2 {
      font-size: 22px;
      margin-bottom: 10px;
    }

    .card p {
      font-size: 15px;
      color: #666;
    }

    @media (max-width: 768px) {
      .container {
        flex-direction: column;
        align-items: center;
      }

      nav {
        flex-direction: column;
        gap: 10px;
      }
    }
  </style>
</head>
<body>

  <nav>
    <div class="brand">🎉 Event Planner</div>
    <div class="user">Welcome, <%= user.name.charAt(0).toUpperCase() + user.name.slice(1)%> </div>
  </nav>

  <section class="hero">
    <h1>Let’s get started!</h1>
    <p>Choose an option to proceed</p>
  </section>

  <div class="container">
    <a href="/create-event" class="card">
      <i class="fas fa-calendar-plus"></i>
      <h2>Create an Event</h2>
      <p>Start organizing your event by setting the date, location, and ticket options.</p>
    </a>

    <a href="/events" class="card">
      <i class="fas fa-ticket-alt"></i>
      <h2>Book a Ticket</h2>
      <p>Browse and book tickets for amazing events happening near you.</p>
    </a>

    <a href="/profile" class="card">
      <i class="fas fa-ticket-alt"></i>
      <h2>Profile</h2>
      <p>View your profile info.</p>
    </a>
  </div>

</body>
</html>
